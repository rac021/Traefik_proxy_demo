port = ":80"
graceTimeOut = 10
logLevel = "INFO"

defaultEntryPoints = ["http"]
entryPoint = "http"

[entryPoints]
  [entryPoints.http]
  address = ":8000"
 
[web]
address = ":8081"
watch = true

[docker]
endpoint = "unix:///var/run/docker.sock"
domain = "docker.localhost"
watch = true

